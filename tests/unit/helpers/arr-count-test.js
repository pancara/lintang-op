import { arrCount } from 'lintang-op/helpers/arr-count';
import { module, test } from 'qunit';

module('Unit | Helper | arr count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrCount([42]);
  assert.ok(result);
});
