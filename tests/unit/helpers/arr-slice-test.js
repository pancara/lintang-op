import { arrSlice } from 'lintang-op/helpers/arr-slice';
import { module, test } from 'qunit';

module('Unit | Helper | arr slice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrSlice([42]);
  assert.ok(result);
});
