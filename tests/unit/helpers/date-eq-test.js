import { dateEq } from 'lintang-op/helpers/date-eq';
import { module, test } from 'qunit';

module('Unit | Helper | date eq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateEq([42]);
  assert.ok(result);
});
