import { dateInlist } from 'lintang-op/helpers/date-inlist';
import { module, test } from 'qunit';

module('Unit | Helper | date inlist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateInlist([42]);
  assert.ok(result);
});
