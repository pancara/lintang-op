import { dateMonthName } from 'lintang-op/helpers/date-month-name';
import { module, test } from 'qunit';

module('Unit | Helper | date month name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateMonthName([42]);
  assert.ok(result);
});
