import { month } from 'lintang-op/helpers/month';
import { module, test } from 'qunit';

module('Unit | Helper | month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = month([42]);
  assert.ok(result);
});
