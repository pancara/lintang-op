import arrayUtil from 'lintang-op/utils/array-util';
import { module, test } from 'qunit';

module('Unit | Utility | array util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayUtil();
  assert.ok(result);
});
